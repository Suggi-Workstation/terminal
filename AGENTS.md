# AGENTS.md - hub-main

## Role
This repo is the navigation hub. It holds the map, not content.

## Boundaries
- ALWAYS: keep README.md accurate when repos are added or renamed.
- ASK FIRST: before adding any new file to this repo.
- NEVER: store knowledge content here (that belongs in hub-brain),
  modify `.github/`, or commit non-ASCII characters.

## Workspace hygiene
- Temp files go in `.agent_temp/` only. Delete them when done.
- If a command fails 3 times in a row: STOP, show the exact error, ask Suggi.
